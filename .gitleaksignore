# Gitleaks findings we've explicitly acknowledged and can't un-commit.
#
# Each line is a fingerprint produced by `gitleaks detect` in the form
# <commit-sha>:<file>:<rule>:<line>. The referenced lines have been redacted
# in later commits but remain in the PR's history; ignoring them here so
# the security lane stays green without rewriting branch history.
#
# The leaked AWS access key ID was already public (pasted in operator chat
# prior to this repo work) and is tracked for rotation in the auto-memory
# file. Rotating the key at iam.aws.amazon.com is the real mitigation;
# this file just silences the scanner's correct-but-redundant finding.

# Initial CI commit (cc0e0dc) — key ID quoted in runbook + discovery docs;
# both have been redacted in follow-up commits.
cc0e0dc494bb74e156ca7c28141a3464398a11e9:.github/CI_RUNBOOK.md:aws-access-token:149
cc0e0dc494bb74e156ca7c28141a3464398a11e9:.github/CI_DISCOVERY.md:aws-access-token:160
